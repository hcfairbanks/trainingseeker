<%= form_for(course_name) do |f| %>
  <% if course_name.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(course_name.errors.count, "error") %> prohibited this course_name from being saved:</h2>

      <ul>
      <% course_name.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
